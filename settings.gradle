pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            mavenContent {
                url uri("${rootProject.projectDir}/repo")
                includeGroup "nexstra"
                includeGroupByRegex 'nexstra\\..*'
            }
        }
    }
}
rootProject.name = 'workdocs'
